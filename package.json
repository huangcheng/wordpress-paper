{
  "name": "wordpress-paper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "wp-env start",
    "build:i18n": "for file in `find . -name \"*.po\"` ; do msgfmt -o ${file/.po/.mo} $file ; done",
    "build:archive": "zip -r9 ./dist.zip *.php assets/* languages/*.mo template-parts/* style.css",
    "build": "npm run build:i18n && npm run build:archive",
    "stop": "wp-env stop"
  },
  "keywords": [],
  "author": "Cheng Huang",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/env": "^9.5.0"
  }
}
